# simple-mo2po

## Build & Test

### Make

```bash
make && make test
```

### Autotools

Currently work in progress.

### Cmake

```bash
mkdir -p build
cd build && cmake ..
make && make test
```

### Meson

```bash
meson build
cd build
meson compile && meson test
```
